# Folder: src/core/

> Trigger: Update when task execution logic or workflow changes.

Core engine for executing tasks, handling multi-step workflows, and polling for completion.
[IN] Task Object, Adapter -> [OUT] Execution Result, Status Updates.
Invariant: TaskExecutor is the single source of truth for "How a task runs".

## Core Files
- task-executor.ts: Manages the lifecycle of a task execution (Single & Multi-step).
