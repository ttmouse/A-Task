# Folder: src/storage/

> Trigger: Update when data schema or persistence mechanism changes.

Wrapper around `chrome.storage.local` for type-safe data access.
[IN] Task Objects -> [OUT] Persisted Data.
Invariant: All direct `chrome.storage` calls should be encapsulated here.

## Core Files
- task-storage.ts: CRUD operations for Tasks and Steps.
