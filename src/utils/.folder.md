# Folder: src/utils/

> Trigger: Update when utility scope changes.

Pure functional helpers for parsing, formatting, and DOM utilities.
[IN] Raw Strings/DOM -> [OUT] Parsed Objects/HTML.
Side-effect free (mostly).

## Core Files
- markdown-renderer.ts: Renders Markdown to HTML.
- task-parser.ts: Parses raw prompt text into Task objects.
